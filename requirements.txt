# --- config ---
pydantic-settings==2.5.2

# --- orchestration ---
langgraph==0.2.35
langchain==0.3.4
langchain-community==0.3.3
langchain-ollama==0.2.0

# --- tools ---
duckduckgo-search==6.3.2
# Unpinned: the mcp package's own CLI extras (fastmcp-slim) and other transitive
# deps in this tree need httpx>=0.28.1 — same reasoning as the other unpinned
# infra packages below.
httpx

# --- mcp ---
# claude.md pins mcp==1.1.0, but fastmcp==2.0.0 requires mcp>=1.6.0,<2.0.0 — an
# unpinned mcp here lets fastmcp pull in whatever compatible version it needs.
mcp
fastmcp==2.0.0

# --- api ---
# Unpinned: fastmcp==2.0.0 needs fastapi>=0.115.12, arize-phoenix==20.4.0 needs
# fastapi>=0.137.0 — same reasoning as the unpinned mcp entry above, let the
# stricter dependency win instead of chasing every future bump by hand.
fastapi
uvicorn

# --- observability ---
# claude.md pins arize-phoenix==4.29.0, but that release is ~18 months stale against
# today's PyPI: its unpinned transitive deps (arize-phoenix-evals' internal module
# layout, sqlean-py's published wheels, pandas dropping a hard pytz dependency) have
# all moved on and broken it — see README "Issues Faced" for the exact chain. Same
# tool, same purpose (LLM tracing), just a version that actually installs and imports
# today.
arize-phoenix==20.4.0
# Added beyond claude.md's list: arize-phoenix alone does not auto-instrument LangChain/LangGraph.
# These are required for Stage 8's tracing to actually capture agent LLM calls.
openinference-instrumentation-langchain>=0.1.29
opentelemetry-exporter-otlp-proto-grpc>=1.25.0

# --- testing ---
pytest==8.3.3
